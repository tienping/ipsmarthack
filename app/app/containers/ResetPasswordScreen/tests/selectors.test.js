// import { fromJS } from 'immutable';
// import { selectResetPasswordScreenDomain } from '../selectors';

describe('selectResetPasswordScreenDomain', () => {
    it('Expect to have unit tests specified', () => {
        expect(true).toEqual(false);
    });
});
