import characterStyle from 'style/characterStyle';
import colorPalette from 'style/colorPalette';

// TP's experiment on making colorPalette and chracterStyle more popular

const hermoStyle = {
    primaryColor: colorPalette.TyrianPurple,
    secondaryColor: colorPalette.Razzmatazz,

    bodyStyle: characterStyle.Body,
};

export default hermoStyle;
